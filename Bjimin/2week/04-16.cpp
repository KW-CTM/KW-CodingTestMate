#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {

	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	int N, i, j, temp;
    vector<int> numbers;

    cin>>N;
    for(i=0; i<N; i++){
        cin>>temp;
        numbers.push_back(temp);
    }

    
}